#include "front-driver.h"
float FrontDriver::getRadius()
{
    // this will divide into 2 radius, inner and outer of the front wheel

    // calculate center point of the car, by intersec backwheel line with the perpencicular to left front wheel when rotating 45 deg

    // calculate rotation angle of the other front wheel

    // stop the moving
    // apply rotation 45 deg, and other calculate angle to front wheel
    // stop rotation
    // do moving wheel
    return 0;
}